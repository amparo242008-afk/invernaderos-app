<!DOCTYPE html>
<html lang="es">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Dashboard — Invernaderos</title>
  <link rel="preconnect" href="https://fonts.googleapis.com">
  <link href="https://fonts.googleapis.com/css2?family=Fraunces:wght@500;600&family=Work+Sans:wght@400;500;600&display=swap" rel="stylesheet">
  <link rel="stylesheet" href="/css/style.css">
</head>
<body>
  <div class="layout">
    <aside class="sidebar">
      <p class="brand-mark">Parque ambiental</p>
      <nav>
        <a href="/dashboard" class="active">Áreas</a>
        <a href="/llamados">Llamados</a>
        <a href="/reportes">Reportes</a>
        <% if (usuario.rol === 'administrador') { %>
        <a href="/empleados">Empleados</a>
        <a href="/usuarios">Usuarios</a>
        <a href="/parametros">Parámetros</a>
        <% } %>
      </nav>
      <div class="user-box">
        <p><%= usuario.nombre || usuario.username %></p>
        <a href="/logout">Salir</a>
      </div>
    </aside>

    <main class="content">
      <header class="content-header">
        <h1>Estado de las áreas</h1>
      </header>

      <section class="areas-grid">
        <% areas.forEach(area => { %>
          <article class="area-card">
            <h2><%= area.nombre %></h2>
            <% if (area.tipo) { %>
              <p class="area-metrics">
                <%= area.valor !== null && area.valor !== undefined ? area.valor : '—' %>
                <span class="area-metric-label"><%= area.tipo === 'temperatura' ? '°C' : area.tipo === 'humedad' ? '%' : area.tipo %></span>
              </p>
              <p class="area-status">
                <span class="dot"></span> En rango
              </p>
            <% } else { %>
              <p class="area-metrics">—</p>
              <p class="area-status"><span class="dot"></span> Sin sensores</p>
            <% } %>
            <p class="area-responsable">
              <%= area.responsable_nombre ? `Responsable: ${area.responsable_nombre} ${area.responsable_apellido}` : 'Sin responsable asignado' %>
            </p>
          </article>
        <% }) %>
        <% if (areas.length === 0) { %>
          <p>No hay áreas cargadas todavía.</p>
        <% } %>
      </section>

      <section class="llamados-section">
        <h2>Últimos llamados</h2>
        <table class="llamados-table">
          <thead>
            <tr><th>Área</th><th>Tipo</th><th>Origen</th><th>Estado</th><th>Fecha</th></tr>
          </thead>
          <tbody>
            <% llamados.forEach(l => { %>
              <tr>
                <td><%= l.area_nombre %></td>
                <td class="tipo-<%= l.tipo %>"><%= l.tipo %></td>
                <td><%= l.origen %></td>
                <td class="<%= l.atendido ? 'badge-atendido' : 'tipo-emergencia' %>"><%= l.atendido ? 'Atendido' : 'Sin atender' %></td>
                <td><%= new Date(l.fecha_hora).toLocaleString('es-AR') %></td>
              </tr>
            <% }) %>
          </tbody>
        </table>
      </section>
    </main>
  </div>
</body>
</html>
